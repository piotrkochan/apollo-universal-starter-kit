#import "./Storage.graphql"

query storages($limit: Int!, $after: Int) {
  storages(limit: $limit, after: $after) {
    totalCount
    edges {
      cursor
      node {
        ...StorageInfo
      }
    }
    pageInfo {
      endCursor
      hasNextPage
    }
  }
}
